export interface SportList {
    betDelayTime: number
    highlight: boolean
    id: number
    maxOddLimit: number
    maxStackLimit: number
    minOddLimit: number
    sequence: number
    sportName: string
    status: boolean
}
