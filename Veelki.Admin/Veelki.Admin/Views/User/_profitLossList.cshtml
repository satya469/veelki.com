@model IEnumerable<Veelki.Model.Model.CreditReferenceVM>
<style>
    #ProfitLossTbl_length {
        float: left;
    }

    #ProfitLossTbl_filter {
        float: right;
    }
</style>
<div class="modal-content">
    <div class="modal-header">
        <h5 class="modal-title">Profit Loss </h5>
        <button type="button" class="close hideModel1">
            ×
        </button>
    </div>
    <div class="modal-body">
        <table id="ProfitLossTbl" class="table table-striped table-bordered" style="width:100%">
            <thead>
                <tr>
                    <th class="th-sm">
                        <label>Sports</label>
                    </th>
                    <th class="th-sm">
                        <label>P/L</label>
                    </th>

                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>
                        Cricket
                    </td>
                    <td>
                        -1000
                    </td>
                </tr>
                <tr>
                    <td>
                        tenis
                    </td>
                    <td>
                        +1000
                    </td>
                </tr>
                @*@if (Model.Count() > 0)
        {
            foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.FromName
                    </td>
                    <td>
                        @item.ToName
                    </td>

                </tr>
            }
        }
        else
        {
        <td class="text-center" colspan="4">No records Found</td>
        }*@
            </tbody>
            </table>
        </div>
    </div>

    <script src="https://cdn.datatables.net/1.11.0/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.11.0/js/dataTables.bootstrap.min.js"></script>
    <script type="text/javascript">
        $(function () {

            $(".hideModel1").click(function () {
                $("#PLModelPopup").modal("hide");
            });
            $("#ProfitLossTbl").DataTable();
            $(".dataTables_length").addClass("bs-select");
        });
    </script>
