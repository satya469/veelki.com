@model IEnumerable<RB444.Model.Model.CreditReferenceVM>
<style>
    #CreditReferenceTbl_length {
        float: left;
    }

    #CreditReferenceTbl_filter {
        float: right;
    }
</style>
<div class="modal-content">
    <div class="modal-header">
        <h5 class="modal-title">Credit Reference Log </h5>
        <button type="button" class="close hideModel1">
            ×
        </button>
    </div>
    <div class="modal-body">
        <table id="CreditReferenceTbl" class="table table-striped table-bordered" style="width:100%">
            <thead>
                <tr>
                    <th class="th-sm">
                        <label>From Name</label>
                    </th>
                    <th class="th-sm">
                        <label>User Name</label>
                    </th>

                    <th class="th-sm">
                        <label>Old Credit Reference</label>
                    </th>
                    <th class="th-sm">
                        <label>New Credit Reference</label>
                    </th>

                </tr>
            </thead>
            <tbody>
                @if (Model.Count() > 0)
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @item.FromName
                            </td>
                            <td>
                                @item.ToName
                            </td>
                            <td>
                                @item.OldBalance
                            </td>
                            <td>
                                @item.NewBalance
                            </td>
                        </tr>
                    }
                }
                else
                {
                <td class="text-center" colspan="4">No records Found</td>
            }
                </tbody>
            </table>
        </div>
    </div>

    <script src="https://cdn.datatables.net/1.11.0/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.11.0/js/dataTables.bootstrap.min.js"></script>
    <script type="text/javascript">
        $(function () {

            $(".hideModel1").click(function () {
                $("#CreditModelPopup").modal("hide");
            });
            $("#CreditReferenceTbl").DataTable();
            $(".dataTables_length").addClass("bs-select");
        });
    </script>